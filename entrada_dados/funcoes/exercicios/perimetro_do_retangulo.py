def perimetro_do_retangulo():

    base = float(input(f'base: {numero}'))
    altura = float(input(f'altura: {numero}'))
    perimetro = 2 * (base + altura)
    print(f'perimetro: {perimetro}')

perimetro_do_retangulo()

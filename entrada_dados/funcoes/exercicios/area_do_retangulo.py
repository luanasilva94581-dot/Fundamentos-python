def area_do_retangulo(numero):
    base = float(input(f'base: {numero}'))
    altura = float(input(f'altura: {numero}'))
    area = base * altura
    print(f'{area_do_retangulo()}')

area_do_retangulo()